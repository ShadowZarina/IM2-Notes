# I. Introduction to MySQL Quiz
1. Name of the table created in SQL Student Database (Scenario)
2. Column that serves as primary key in the table
3. Database and size of StudID
4. Make value automatically increase -> AUTO-INCREMENT
5. What happens if you try to insert duplicate value into StudID? -> throw error due to 'UNIQUE' constraint
6. What does 'SELECT * FROM student_database' do?
7. What columns are displayed in 'SELECT StudID, fname, lname from student_database? -> all 3
8. Why was 'student_id' column excluded from query output? -> NOT SPECIFIED
9. Why is student_id omitted from INSERT INTO command? -> auto-incremenet, automatically added
10. studID for student named Khugo
11. Lname of Andrei
12. Separates multiple rows in INSERT INTO -> comma (,)
13. Syntax error if semicolon is used instead of comma in SELECT query? -> trailing comma expects more values / should end in semicolon
14. Offline environment/tool ussed to run SQL Queries -> phpMyAdmin
15. Constraint tht ensures fname and lname can't be left blank -> NOT NULL

CONSTRAINTS: unique, primary key, not null, auto-increment
